# 00 · 环境与工程结构

> 工程说明文档（持久保留，非代码注释）。记录开发环境、工程结构、关键决策。
> 本工程 = 九条会大前端。设计文档在角色卡工程 `D:\application\tavern-projects\jiutiao\_design\`。

## 开发环境（已搭建）

| 工具 | 版本/位置 | 备注 |
|------|----------|------|
| Node.js | v24.16 | 系统已装 |
| npm | 11 | 全局目录/缓存已重定向到 `D:\devtools\npm-global` / `D:\devtools\npm-cache` |
| pnpm | 11.6，装在 `D:\devtools\npm-global` | store 在 `D:\devtools\pnpm-store`，均在 D 盘 |
| git | 2.54 | 本工程为独立纯净仓库 |
| Vite | 8.x | 构建/开发服务器 |
| React + TS | 19 / 6.x | 见 ADR-001（设计文档大前端架构规划） |
| VS Code | **未装成功** | 微软下载域名在本网络被 ECONNRESET 阻断，待用代理/镜像另装。不影响开发 |

### 环境注意
- `D:\devtools\npm-global` 已加入用户 PATH（新终端 pnpm 直接可用）。
- 当前会话若 pnpm 不可用，先 `export PATH="/d/devtools/npm-global:$PATH"`。
- npm registry 直连可用（registry.npmjs.org 200；备用 registry.npmmirror.com）。
- 微软 VS Code 下载 CDN 不通——VS Code 需另行用代理或国内镜像安装；不阻塞工程。

## 工程结构

```
jiutiao-frontend/
  src/
    sillytavern/    底座层(将从 sillytavern-web skill 模板复制)：
                    database/lorebook-engine/api-router/stream-parser/variables/prompt-assembler...
    game/           九条会业务层(我们写)：
      action-grid/  行动格系统(分配/逐格提交/校验/时序)
      paradigm/     范式筛选(首次判定/账本/排除)
      economy/      经济结算(避孕套/行动格/威望/欲望/武力公式·纯函数)
      corruption/   堕落度(首次特殊事件累计→认知防线→奖励闸门)
      events/       事件系统(候选池/优先级/触发)
      endings/      结局判定
      schema.ts     从角色卡工程复制(单一真相源)
    components/
      game/         九条会专属UI(行动格界面/地图/状态栏)
    hooks/
  docs/             工程说明文档(本目录)
  tests/            单元测试
```

### 分层规则（code-craft）
表现层(components) → 业务层(game/hooks) → 数据层(sillytavern/schema)。上依赖下，下不反向依赖上。
game/ 内经济/范式/堕落尽量纯函数，易测易回退。

## 前后端职责（核心定位锚·不可违反）
- 代码层(本前端)：行动格/范式筛选/经济结算/事件/堕落/结局/每日审查/存档——所有硬逻辑。
- AI1(主API)：按代码喂的范式扩写正文。
- AI2(次API)：抓数值算变量（前端校验防胡诌）。
- 酒馆世界书：范式模板库/角色设定，作为内容被前端注入prompt。

## Git/文档纪律
- 按功能节点 commit，便于回退；关键节点打 tag。
- 每个 game/ 子模块落成，在 docs/ 留独立说明（做什么/接口/关键决策/依赖关系）。
- dist、node_modules 不入库。

## 里程碑记录
- ✅ M0 环境搭建：Vite+React+TS 脚手架跑通（`pnpm build` 88ms 成功），git init + 首次commit，目录骨架建立。
- ⬜ M1 schema对接
- ⬜ M2 行动格系统
- ⬜ ...（见设计文档 大前端架构规划_v1.md 第5节）
